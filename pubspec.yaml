name: firstflutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0

  cupertino_icons: ^1.0.2
  firebase_auth: ^4.2.0
  google_fonts_arabic: ^0.0.5
     # git: git://github.com/emadomedher/google-fonts-arabic.git
    #  version: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  fonts:
    - family: ElMessiri
      fonts:
  assets:
    - assets/fonts/ElMessiri-VariableFont_wght.ttf
    - assets/images/housebg.png